pipeline{
    agent any
    stages{
        stage(build){
            steps{
                CurrentBuild.displayName="Deployments"
                CurrentBuild.description="Used"
            }
        }
    }
}

